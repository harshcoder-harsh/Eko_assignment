# Test-only dependencies, on top of requirements.txt.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# These were previously undeclared, so a fresh clone could not run the suite
# even after a successful `pip install -r requirements.txt`.

pytest==9.1.1
httpx==0.28.1        # required by fastapi.testclient